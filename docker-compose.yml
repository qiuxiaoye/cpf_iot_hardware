services:
  mosquitto:
    image: eclipse-mosquitto
    container_name: mosquitto-iot
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto/config/mosquitto.conf:/mosquitto/config/mosquitto.conf
      - ./mosquitto/data:/mosquitto/data
      - ./mosquitto/log:/mosquitto/log
    restart: unless-stopped
    environment:
      - TZ=Asia/Singapore
    networks:
      - mqtt-network

  paho_mqtt:
    build: ./paho_mqtt
    image: cpf-iot/hardware
    container_name: cpf_iot_runner
    depends_on:
      - mosquitto
    networks:
      - mqtt-network
    restart: unless-stopped

  zigbee2mqtt:
    image: koenkk/zigbee2mqtt
    container_name: zigbee2mqtt-iot
    ports:
      - "8080:8080"
    volumes:
      - ./zigbee2mqtt/data/configuration.yaml:/app/data/configuration.yaml
      - ./zigbee2mqtt/log:/app/data/log
      # - ./zigbee2mqtt/log:/app/log
    devices:
      - /dev/ttyUSB0:/dev/ttyUSB0
    environment:
      - TZ=Asia/Singapore
    restart: unless-stopped
    depends_on:
      - mosquitto
    networks:
      - mqtt-network

networks:
  mqtt-network:
    driver: bridge
